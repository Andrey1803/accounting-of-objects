# DOCX → PDF для паспорта скважины (LibreOffice headless на Railway/Linux)
# «...» — сохранить стандартные пакеты nixpacks; writer+common легче мета-пакета libreoffice
[phases.setup]
aptPkgs = ["...", "libreoffice-writer", "libreoffice-common", "fonts-dejavu-core"]
